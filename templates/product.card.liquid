{%- layout none -%}
{% case settings.design %}
	{% when 'alternate' %}
		{%- render 'product-card-new', product: product -%}
	{% when 'standard' %}
		{%- render 'product-card', product: product -%}
{% endcase %}
